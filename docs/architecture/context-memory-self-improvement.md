# Context, Memory, and Self-Improvement

Memory planes: project, workspace, organization, optional global.

Candidate lifecycle:

```text
draft -> candidate -> safety_scanned -> evaluated -> approved -> active -> monitored
```

No memory/prompt/retrieval/self-improvement change should be promoted unless it improves measured performance without regression. Rejected items go to quarantine.
